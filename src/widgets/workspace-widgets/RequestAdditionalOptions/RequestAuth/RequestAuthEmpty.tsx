import "./styles/RequestAuth.scss";

const RequestAuthEmpty = () => (
	<div className="auth_empty">
		<div className="auth_empty_text">This request does not have authorization...</div>
	</div>
);

export default RequestAuthEmpty;
